root = "."
tmp_dir = "build"

[build]

cmd = "go build -o ./build/main /app/cmd/main.go"
bin = "build/main"

include_ext = ["go", "tpl", "tmpl", "html"]
exclude_dir = ["assets", "tmp", "vendor", "frontend/node_modules"]

poll = true
poll_interval = 500